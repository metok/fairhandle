# Milestones

| # | Plan | Status |
|---|------|--------|
| 1 | Domain core + in-process happy-path e2e | shipped (tag: plan-1-milestone) |
| 2 | Dispute + deadlock + walk-away + hard limits | shipped (tag: plan-2-milestone) |
| 3 | Real adapters: Ed25519, JCS strict, WS-localhost, SQLite, Anthropic | shipped (tag: plan-3-milestone) |
| 4 | Git projection + verifier CLI + export bundle | shipped (tag: plan-4-milestone) |
| 5 | MCP server + Claude Desktop config + HTTP endpoint | shipped (tag: plan-5-milestone) |
| 6 | Web UI (per-peer view + dual-pane observer) | shipped (tag: plan-6-milestone) |
| 7 | Material-equivalence consolidation + transcript visibility | shipped (tag: plan-7-milestone) |
| 8 | Neutral mediator role: single canonical consolidator, peer audit + veto, deadlock policy, three-party Merkle convergence | shipped (tag: plan-8-milestone) |
| 9 | Mediator as own MCP-server process: broadcasting WebSocket hub (`WebSocketHubChannel`) with history replay, channel/LLM injection in `RoomRegistry`, `get_mediator_identity` + `join_as_mediator` tools, mediator-aware consolidation loops (`driveMediatorLoop`, `drivePeerReviewLoop`), three-party eval harness + demo | shipped |
